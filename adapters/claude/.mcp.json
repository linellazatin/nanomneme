{
  "mcpServers": {
    "memory": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/server.js"],
      "env": {
        "NMNM_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
